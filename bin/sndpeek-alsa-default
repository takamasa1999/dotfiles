#!/usr/bin/env zsh
set -euo pipefail

DEFAULT_INPUT_NUMBER=$(
    sndpeek-alsa --help 2>&1 |
        awk -F: '/default channels/{print $1}' |
        tr -d '[:space:]'
)

sndpeek-alsa --inputDevice:$DEFAULT_INPUT_NUMBER
# --fullscreen:ON
